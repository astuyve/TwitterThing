<%= form_tag('/search', method: 'get', html: {class: "form", role: "form"}) do |f| %>
  <ul class="nav navbar-text pull-right">
  <div class="form-group">
    <li><%= label_tag(:search_terms, "Search twitter for:") %></li>
    <%= text_field_tag(:search_terms) %>
  </div>
  <div class="actions">
    <li><%= submit_tag("Search", :class=>"btn btn-large btn-default") %></li>
  </div>
  </ul>
<% end %>
