<%= form_tag('/search', method: 'get', html: {class: "form", role: "form"}) do |f| %>
  <div class="form-group">
    <%= label_tag(:search_terms, "Search twitter for:") %>
    <%= text_field_tag(:search_terms) %>
  </div>
  <div class="actions">
    <%= submit_tag("Search") %>
  </div>
<% end %>
